# now: the options for filters receives by filter routes. 
# We will use the same logic, so we will split our filters to abnormality and just filter (as they did) 
# and remove the patient_id filter (or replace it with empi_annon).
# 




# see what each toute is for and which function should be in data_manager.py
# write our functions in data_manager.py
# see if it works with their electrron