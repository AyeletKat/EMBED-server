# When initializing the viewer - gets the pictures by their Id (number of the row their path in)
# When filtering - electron gives list of filters -> 
#                  server filters on metadata and gives list of picture_ID that goes through the filter ->
#                  electron ask the images by their IDs and the metadata of the pictures also by their ID.
#  